# tl;dr -- can we build a docker image successfully?

include:
  - local: ".gitlab/cicd/docker-setup.yml"

docker-build-smoke:
  extends: .docker-setup
  stage: "test"
  script:
    - make docker-uncached
    - docker run ot-tools --help
